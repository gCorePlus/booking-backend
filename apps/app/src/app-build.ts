export default {
  version: '%VERSION%',
  datetime: '%BUILD_DATE_TIME%',
  git: {
    branch: '%GIT_BRANCH%',
    hash: '%GIT_HASH%',
    abbreviatedSha: '%GIT_SHORT_HASH%'
  },
  dependencies: {
  }
};
